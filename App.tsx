import React, { useState, useMemo, memo, useEffect, useRef } from 'react';
import NeoButton from './components/NeoButton';
import { Level, Lesson, UserProgress, Quiz } from './types';
import { generateCustomRoadmap, generateRankExam } from './services/geminiService';

type ViewState = 'START' | 'AUTH' | 'HOME' | 'ONBOARDING' | 'LOADING' | 'ROADMAP' | 'LESSON' | 'QUIZ' | 'PROFILE' | 'QUIZ_SUMMARY' | 'RANK_EXAM' | 'RANK_SUCCESS' | 'STORE' | 'SETTINGS';
type Language = 'en' | 'te' | 'kn' | 'ml' | 'ta' | 'hi';

interface Category {
  id: string;
  title: string;
  icon: string;
  description: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
  topics: {
    level1: string;
    level2: string;
    level3: string;
  };
}

interface StoreItem {
  id: string;
  name: string;
  type: 'banner' | 'badge';
  cost: number;
  description: string;
  value: string; // CSS class for banner, Emoji/Text for badge
}

const STORE_ITEMS: StoreItem[] = [
  { id: 'banner_classic', name: 'Classic Green', type: 'banner', cost: 0, description: 'The standard look.', value: 'bg-[#90EE90]/20' },
  { id: 'banner_fire', name: 'Inferno', type: 'banner', cost: 500, description: 'Blazing path.', value: 'bg-gradient-to-r from-red-500/20 to-yellow-500/20' },
  { id: 'banner_ocean', name: 'Deep Ocean', type: 'banner', cost: 500, description: 'Calm waters.', value: 'bg-gradient-to-r from-blue-500/20 to-cyan-400/20' },
  { id: 'banner_gold', name: 'Midas Touch', type: 'banner', cost: 1000, description: 'Pure luxury.', value: 'bg-gradient-to-r from-yellow-400/30 to-yellow-600/30' },
  { id: 'banner_void', name: 'The Void', type: 'banner', cost: 2000, description: 'For the elite.', value: 'bg-black/10' },
  
  { id: 'badge_bull', name: 'Bull Market', type: 'badge', cost: 250, description: 'Optimistic!', value: '📈' },
  { id: 'badge_bear', name: 'Bear Hug', type: 'badge', cost: 250, description: 'Safe & Sound.', value: '🐻' },
  { id: 'badge_rich', name: 'Rich Cat', type: 'badge', cost: 500, description: 'Meow money.', value: '😼' },
  { id: 'badge_diamond', name: 'Diamond Hands', type: 'badge', cost: 1000, description: 'Hold tight.', value: '💎' },
  { id: 'badge_rocket', name: 'To The Moon', type: 'badge', cost: 1500, description: 'No limits.', value: '🚀' },
];

const LOCALIZED_CONTENT = {
  en: {
    categories: [
      { 
        id: 'money-basics', 
        title: '1. MONEY BASICS', 
        description: 'Understand money before touching banks or apps. Income, Budgeting, and Inflation.',
        topics: {
          level1: "1.1 Income & Expenses (Income types vs Needs & Wants)",
          level2: "1.2 Budgeting & Cash Flow (Monthly rules & beginner mistakes)",
          level3: "1.3 Inflation & Value of Money (Why money loses value & why saving isn't enough)"
        }
      },
      { 
        id: 'banking', 
        title: '2. BANKING', 
        description: 'Use banks confidently. Accounts, bank business models, and daily operations.',
        topics: {
          level1: "2.1 Bank Accounts (Savings, current, salary, interest, statements)",
          level2: "2.2 How Banks Earn Money (Deposits vs Loans, interest margins, penalties)",
          level3: "2.3 Banking Operations (Cheques, ATM, Net banking, KYC, RBI basics)"
        }
      },
      { 
        id: 'digital-payments', 
        title: '3. DIGITAL PAYMENTS', 
        description: 'Daily digital money usage without fear. Mastering UPI and secure transfers.',
        topics: {
          level1: "3.1 How UPI Works (Sender to Bank flow, UPI apps & limits)",
          level2: "3.2 Transfers & Payments (UPI vs IMPS/NEFT/RTGS, QR codes)",
          level3: "3.3 Failed Payments & Refunds (Fail reasons, timelines, escalation)"
        }
      },
      { 
        id: 'saving-investing', 
        title: '4. SAVING & INVESTING', 
        description: 'Make money grow safely. Compounding, risk management, and saving tools.',
        topics: {
          level1: "4.1 Saving Options (Savings vs FD vs RD, when to use which)",
          level2: "4.2 Returns & Compounding (Simple vs Compound, time advantage)",
          level3: "4.3 Risk Basics (Low/Medium/High risk, Inflation vs Returns)"
        }
      },
      { 
        id: 'loans-credit', 
        title: '5. LOANS & CREDIT', 
        description: 'Borrow smart, avoid debt traps. EMIs, loan types, and credit scores.',
        topics: {
          level1: "5.1 Types of Loans (Personal/Home/Education, Secured vs Unsecured)",
          level2: "5.2 EMIs & Interest (EMI structure, Flat vs Reducing balance)",
          level3: "5.3 Credit Cards & Credit Score (How cards work, CIBIL basics)"
        }
      },
      { 
        id: 'safety-protection', 
        title: '6. SAFETY & PROTECTION', 
        description: 'Protect your money and future. Scams, taxes, and insurance basics.',
        topics: {
          level1: "6.1 Online Safety & Scams (UPI/OTP/Phishing, what to do if scammed)",
          level2: "6.2 Tax Basics (Why tax exists, PAN, slabs, Form 16)",
          level3: "6.3 Insurance & Protection (Health vs Life, Nominee & Documentation)"
        }
      }
    ],
    welcomeLesson: {
      title: "Welcome to FINQUEST",
      content: "FINQUEST is your personal guide to financial freedom. Start by picking a path that suits your current needs. We suggest starting with Money Basics if you are new!",
      quizQuestion: "What is the best way to start your financial journey?",
      options: ["Ignoring expenses", "Learning the basics", "Gambling", "Doing nothing"]
    }
  },
  te: {
    categories: [
      { 
        id: 'money-basics', 
        title: '1. డబ్బు మౌలిక అంశాలు', 
        description: 'బ్యాంకులు లేదా యాప్‌లను తాకే ముందు డబ్బును అర్థం చేసుకోండి. ఆదాయం, బడ్జెట్ మరియు ద్రవ్యోల్బణం.',
        topics: {
          level1: "1.1 ఆదాయం & ఖర్చులు (ఆదాయ రకాలు vs అవసరాలు & కోరికలు)",
          level2: "1.2 బడ్జెట్ & నగదు ప్రవాహం (నెలవారీ నియమాలు & ప్రారంభ తప్పులు)",
          level3: "1.3 ద్రవ్యోల్బణం & డబ్బు విలువ (డబ్బు విలువ ఎందుకు తగ్గుతుంది & పొదుపు ఎందుకు సరిపోదు)"
        }
      },
      { 
        id: 'banking', 
        title: '2. బ్యాంకింగ్', 
        description: 'బ్యాంకులను నమ్మకంగా ఉపయోగించండి. ఖాతాలు, బ్యాంక్ వ్యాపార నమూనాలు మరియు రోజువారీ కార్యకలాపాలు.',
        topics: {
          level1: "2.1 బ్యాంక్ ఖాతాలు (పొదుపు, కరెంట్, జీతం, వడ్డీ, స్టేట్‌మెంట్‌లు)",
          level2: "2.2 బ్యాంకులు డబ్బు ఎలా సంపాదిస్తాయి (డిపాజిట్లు vs రుణాలు, వడ్డీ మార్జిన్‌లు)",
          level3: "2.3 బ్యాంకింగ్ కార్యకలాపాలు (చెక్, ATM, నెట్ బ్యాంకింగ్, KYC)"
        }
      },
      { 
        id: 'digital-payments', 
        title: '3. డిజిటల్ చెల్లింపులు', 
        description: 'భయం లేకుండా రోజువారీ డిజిటల్ డబ్బు వినియోగం. UPI మరియు సురక్షిత బదిలీలను నేర్చుకోండి.',
        topics: {
          level1: "3.1 UPI ఎలా పనిచేస్తుంది (UPI యాప్‌లు & పరిమితులు)",
          level2: "3.2 బదిలీలు & చెల్లింపులు (UPI vs IMPS/NEFT, QR కోడ్‌లు)",
          level3: "3.3 విఫలమైన చెల్లింపులు & వాపసు (కారణాలు, కాలపరిమితి)"
        }
      },
      { 
        id: 'saving-investing', 
        title: '4. పొదుపు & పెట్టుబడి', 
        description: 'డబ్బును సురక్షితంగా పెరిగేలా చేయండి. కాంపౌండింగ్, రిస్క్ మేనేజ్‌మెంట్ మరియు పొదుపు సాధనాలు.',
        topics: {
          level1: "4.1 పొదుపు ఎంపికలు (Savings vs FD vs RD)",
          level2: "4.2 రాబడి & కాంపౌండింగ్ (సమయ ప్రయోజనం)",
          level3: "4.3 రిస్క్ బేసిక్స్ (తక్కువ/మధ్యస్థ/అధిక రిస్క్, ద్రవ్యోల్బణం)"
        }
      },
      { 
        id: 'loans-credit', 
        title: '5. రుణాలు & క్రెడిట్', 
        description: 'తెలివిగా అప్పు తీసుకోండి, అప్పుల ఊబిలో పడకండి. EMIలు, రుణ రకాలు మరియు క్రెడిట్ స్కోర్లు.',
        topics: {
          level1: "5.1 రుణ రకాలు (వ్యక్తిగత/గృహ/విద్యా రుణాలు)",
          level2: "5.2 EMIలు & వడ్డీ (EMI నిర్మాణం)",
          level3: "5.3 క్రెడిట్ కార్డులు & సిబిల్ స్కోర్"
        }
      },
      { 
        id: 'safety-protection', 
        title: '6. భద్రత & రక్షణ', 
        description: 'మీ డబ్బు మరియు భవిష్యత్తును రక్షించుకోండి. మోసాలు, పన్నులు మరియు బీమా ప్రాథమిక అంశాలు.',
        topics: {
          level1: "6.1 ఆన్‌లైన్ భద్రత & మోసాలు (UPI/OTP/ఫిషింగ్)",
          level2: "6.2 పన్ను ప్రాథమిక అంశాలు (పాన్, స్లాబ్‌లు)",
          level3: "6.3 బీమా & రక్షణ (ఆరోగ్యం vs జీవితం)"
        }
      }
    ],
    welcomeLesson: {
      title: "ఫిన్‌క్వెస్ట్‌కు స్వాగతం",
      content: "ఫిన్‌క్వెస్ట్ ఆర్థిక స్వేచ్ఛకు మీ వ్యక్తిగత మార్గదర్శి. మీ ప్రస్తుత అవసరాలకు సరిపోయే మార్గాన్ని ఎంచుకోవడం ద్వారా ప్రారంభించండి. మీరు కొత్తవారైతే మనీ బేసిక్స్‌తో ప్రారంభించమని మేము సూచిస్తున్నాము!",
      quizQuestion: "మీ ఆర్థిక ప్రయాణాన్ని ప్రారంభించడానికి ఉత్తమ మార్గం ఏమిటి?",
      options: ["ఖర్చులను విస్మరించడం", "ప్రాథమికాలను నేర్చుకోవడం", "జూదం", "ఏమీ చేయకపోవడం"]
    }
  },
  kn: {
    categories: [
      { 
        id: 'money-basics', 
        title: '1. ಹಣದ ಮೂಲಭೂತ ಅಂಶಗಳು', 
        description: 'ಬ್ಯಾಂಕುಗಳು ಅಥವಾ ಅಪ್ಲಿಕೇಶನ್‌ಗಳನ್ನು ಬಳಸುವ ಮೊದಲು ಹಣವನ್ನು ಅರ್ಥಮಾಡಿಕೊಳ್ಳಿ. ಆದಾಯ, ಬಜೆಟ್ ಮತ್ತು ಹಣದುಬ್ಬರ.',
        topics: {
          level1: "1.1 ಆದಾಯ ಮತ್ತು ವೆಚ್ಚಗಳು (ಆದಾಯ ವಿಧಗಳು vs ಅಗತ್ಯಗಳು)",
          level2: "1.2 ಬಜೆಟ್ ಮತ್ತು ನಗದು ಹರಿವು (ಮಾಸಿಕ ನಿಯಮಗಳು)",
          level3: "1.3 ಹಣದುಬ್ಬರ ಮತ್ತು ಹಣದ ಮೌಲ್ಯ (ಹಣ ಏಕೆ ಮೌಲ್ಯವನ್ನು ಕಳೆದುಕೊಳ್ಳುತ್ತದೆ)"
        }
      },
      { 
        id: 'banking', 
        title: '2. ಬ್ಯಾಂಕಿಂಗ್', 
        description: 'ಬ್ಯಾಂಕುಗಳನ್ನು ಆತ್ಮವಿಶ್ವಾಸದಿಂದ ಬಳಸಿ. ಖಾತೆಗಳು, ಬ್ಯಾಂಕ್ ವ್ಯವಹಾರ ಮಾದರಿಗಳು ಮತ್ತು ದೈನಂದಿನ ಕಾರ್ಯಗಳು.',
        topics: {
          level1: "2.1 ಬ್ಯಾಂಕ್ ಖಾತೆಗಳು (ಉಳಿತಾಯ, ಕರೆಂಟ್, ಸಂಬಳ)",
          level2: "2.2 ಬ್ಯಾಂಕುಗಳು ಹೇಗೆ ಗಳಿಸುತ್ತವೆ (ಠೇವಣಿ vs ಸಾಲ)",
          level3: "2.3 ಬ್ಯಾಂಕಿಂಗ್ ಕಾರ್ಯಗಳು (ಚೆಕ್, ಎಟಿಎಂ, ನೆಟ್ ಬ್ಯಾಂಕಿಂಗ್)"
        }
      },
      { 
        id: 'digital-payments', 
        title: '3. ಡಿಜಿಟಲ್ ಪಾವತಿಗಳು', 
        description: 'ಭಯವಿಲ್ಲದೆ ದೈನಂದಿನ ಡಿಜಿಟಲ್ ಹಣ ಬಳಕೆ. UPI ಮತ್ತು ಸುರಕ್ಷಿತ ವರ್ಗಾವಣೆಗಳನ್ನು ಕರಗತ ಮಾಡಿಕೊಳ್ಳಿ.',
        topics: {
          level1: "3.1 UPI ಹೇಗೆ ಕಾರ್ಯನಿರ್ವಹಿಸುತ್ತದೆ",
          level2: "3.2 ವರ್ಗಾವಣೆಗಳು ಮತ್ತು ಪಾವತಿಗಳು (UPI vs NEFT)",
          level3: "3.3 ವಿಫಲವಾದ ಪಾವತಿಗಳು ಮತ್ತು ಮರುಪಾವತಿ"
        }
      },
      { 
        id: 'saving-investing', 
        title: '4. ಉಳಿತಾಯ ಮತ್ತು ಹೂಡಿಕೆ', 
        description: 'ಹಣವನ್ನು ಸುರಕ್ಷಿತವಾಗಿ ಬೆಳೆಸಿ. ಕಾಂಪೌಂಡಿಂಗ್, ರಿಸ್ಕ್ ಮ್ಯಾನೇಜ್ಮೆಂಟ್ ಮತ್ತು ಉಳಿತಾಯ ಪರಿಕರಗಳು.',
        topics: {
          level1: "4.1 ಉಳಿತಾಯ ಆಯ್ಕೆಗಳು (Savings vs FD)",
          level2: "4.2 ಆದಾಯ ಮತ್ತು ಕಾಂಪೌಂಡಿಂಗ್",
          level3: "4.3 ರಿಸ್ಕ್ ಬೇಸಿಕ್ಸ್ (ಹಣದುಬ್ಬರ vs ಆದಾಯ)"
        }
      },
      { 
        id: 'loans-credit', 
        title: '5. ಸಾಲಗಳು ಮತ್ತು ಕ್ರೆಡಿಟ್', 
        description: 'ಬುದ್ಧಿವಂತಿಕೆಯಿಂದ ಸಾಲ ಮಾಡಿ, ಸಾಲದ ಬಲೆಯಲ್ಲಿ ಬೀಳಬೇಡಿ. EMIಗಳು, ಸಾಲದ ವಿಧಗಳು ಮತ್ತು ಕ್ರೆಡಿಟ್ ಸ್ಕೋರ್‌ಗಳು.',
        topics: {
          level1: "5.1 ಸಾಲದ ವಿಧಗಳು (ವೈಯಕ್ತಿಕ/ಗೃಹ ಸಾಲ)",
          level2: "5.2 EMI ಮತ್ತು ಬಡ್ಡಿ",
          level3: "5.3 ಕ್ರೆಡಿಟ್ ಕಾರ್ಡ್‌ಗಳು ಮತ್ತು ಸಿಬಿಲ್"
        }
      },
      { 
        id: 'safety-protection', 
        title: '6. ಸುರಕ್ಷತೆ ಮತ್ತು ರಕ್ಷಣೆ', 
        description: 'ನಿಮ್ಮ ಹಣ ಮತ್ತು ಭವಿಷ್ಯವನ್ನು ರಕ್ಷಿಸಿ. ವಂಚನೆಗಳು, ತೆರಿಗೆಗಳು ಮತ್ತು ವಿಮೆ ಮೂಲಭೂತ ಅಂಶಗಳು.',
        topics: {
          level1: "6.1 ಆನ್‌ಲೈನ್ ಸುರಕ್ಷತೆ ಮತ್ತು ವಂಚನೆಗಳು",
          level2: "6.2 ತೆರಿಗೆ ಮೂಲಭೂತ (ಪ್ಯಾನ್, ಫಾರ್ಮ್ 16)",
          level3: "6.3 ವಿಮೆ ಮತ್ತು ರಕ್ಷಣೆ (ಆರೋಗ್ಯ vs ಜೀವನ)"
        }
      }
    ],
    welcomeLesson: {
      title: "FINQUEST ಗೆ ಸುಸ್ವಾಗತ",
      content: "FINQUEST ಆರ್ಥಿಕ ಸ್ವಾತಂತ್ರ್ಯಕ್ಕೆ ನಿಮ್ಮ ವೈಯಕ್ತಿಕ ಮಾರ್ಗದರ್ಶಿಯಾಗಿದೆ. ನಿಮ್ಮ ಪ್ರಸ್ತುತ ಅಗತ್ಯಗಳಿಗೆ ಸೂಕ್ತವಾದ ಮಾರ್ಗವನ್ನು ಆರಿಸುವ ಮೂಲಕ ಪ್ರಾರಂಭಿಸಿ. ನೀವು ಹೊಸಬರಾಗಿದ್ದರೆ ಹಣದ ಮೂಲಭೂತಗಳೊಂದಿಗೆ ಪ್ರಾರಂಭಿಸಲು ನಾವು ಸೂಚಿಸುತ್ತೇವೆ!",
      quizQuestion: "ನಿಮ್ಮ ಆರ್ಥಿಕ ಪ್ರಯಾಣವನ್ನು ಪ್ರಾರಂಭಿಸಲು ಉತ್ತಮ ಮಾರ್ಗ ಯಾವುದು?",
      options: ["ವೆಚ್ಚಗಳನ್ನು ನಿರ್ಲಕ್ಷಿಸುವುದು", "ಮೂಲಭೂತ ವಿಷಯಗಳನ್ನು ಕಲಿಯುವುದು", "ಜೂಜಾಟ", "ಏನೂ ಮಾಡದಿರುವುದು"]
    }
  },
  ml: {
    categories: [
      { 
        id: 'money-basics', 
        title: '1. പണത്തിൻ്റെ അടിസ്ഥാനങ്ങൾ', 
        description: 'ബാങ്കുകളോ ആപ്പുകളോ ഉപയോഗിക്കുന്നതിന് മുമ്പ് പണത്തെക്കുറിച്ച് മനസ്സിലാക്കുക. വരുമാനം, ബജറ്റിംഗ്, പണപ്പെരുപ്പം.',
        topics: {
          level1: "1.1 വരുമാനവും ചെലവും",
          level2: "1.2 ബജറ്റിംഗും പണമൊഴുക്കും",
          level3: "1.3 പണപ്പെരുപ്പവും പണത്തിന്റെ മൂല്യവും"
        }
      },
      { 
        id: 'banking', 
        title: '2. ബാങ്കിംഗ്', 
        description: 'ബാങ്കുകൾ ആത്മവിശ്വാസത്തോടെ ഉപയോഗിക്കുക. അക്കൗണ്ടുകൾ, ബാങ്ക് ബിസിനസ് മോഡലുകൾ, ദൈനംദിന പ്രവർത്തനങ്ങൾ.',
        topics: {
          level1: "2.1 ബാങ്ക് അക്കൗണ്ടുകൾ (സേവിംഗ്സ്, കറന്റ്)",
          level2: "2.2 ബാങ്കുകൾ എങ്ങനെ പണം ഉണ്ടാക്കുന്നു",
          level3: "2.3 ബാങ്കിംഗ് പ്രവർത്തനങ്ങൾ (എടിഎം, കെവൈസി)"
        }
      },
      { 
        id: 'digital-payments', 
        title: '3. ഡിಜಿറ്റൽ പേയ്മെന്റുകൾ', 
        description: 'ഭയമില്ലാതെ ദൈനംദിന ഡിജിറ്റൽ പണ ഉപയോഗം. UPI-യും സുരക്ഷിതമായ കൈമാറ്റങ്ങളും പഠിക്കുക.',
        topics: {
          level1: "3.1 യുപിഐ എങ്ങനെ പ്രവർത്തിക്കുന്നു",
          level2: "3.2 പണമിടപാടുകൾ (UPI vs NEFT)",
          level3: "3.3 പരാജയപ്പെട്ട പേയ്‌മെന്റുകൾ"
        }
      },
      { 
        id: 'saving-investing', 
        title: '4. സമ്പാദ്യവും നിക്ഷേപവും', 
        description: 'പണം സുരക്ഷിതമായി വളർത്തുക. കോമ്പൗണ്ടിംഗ്, റിസ്ಕ್ മാനേജ്മെന്റ്, സമ്പാദ്യ ഉപകരണങ്ങൾ.',
        topics: {
          level1: "4.1 സേവിംഗ്സ് ഓപ്ഷനുകൾ",
          level2: "4.2 റിട്ടേണുകളും കോമ്പൗണ്ടിംഗും",
          level3: "4.3 റിസ്ക് അടിസ്ഥാനങ്ങൾ"
        }
      },
      { 
        id: 'loans-credit', 
        title: '5. വായ്പകളും ക്രെഡിറ്റും', 
        description: 'ബുദ്ധിപൂർവ്വം കടം വാങ്ങുക, കടക്കെണി ഒഴിവാക്കുക. EMI-കൾ, വായ്പാ തരം, ക്രെഡിറ്റ് സ്കോറുകൾ.',
        topics: {
          level1: "5.1 വായ്പാ തരങ്ങൾ",
          level2: "5.2 ഇഎംഐയും പലിശയും",
          level3: "5.3 ക്രെഡിറ്റ് കാർഡുകൾ"
        }
      },
      { 
        id: 'safety-protection', 
        title: '6. സുരക്ഷയും സംരക്ഷണവും', 
        description: 'നിങ്ങളുടെ പണവും ഭാവിയും സംരക്ഷിക്കുക. തട്ടിപ്പുകൾ, നികുതികൾ, ഇൻഷുറൻസ് അടിസ്ഥാനങ്ങൾ.',
        topics: {
          level1: "6.1 ഓൺലൈൻ സുരക്ഷ",
          level2: "6.2 നികുതി അടിസ്ഥാനങ്ങൾ",
          level3: "6.3 ഇൻഷുറൻസ് (ആരോഗ്യം vs ജീവൻ)"
        }
      }
    ],
    welcomeLesson: {
      title: "FINQUEST-ലേക്ക് സ്വാഗതം",
      content: "സാമ്പത്തിക സ്വാതന്ത്ര്യത്തിലേക്കുള്ള നിങ്ങളുടെ വഴികാട്ടിയാണ് FINQUEST. നിങ്ങളുടെ ആവശ്യങ്ങൾക്ക് അനുയോജ്യമായ ഒരു വഴി തിരഞ്ഞെടുത്ത് തുടങ്ങുക. നിങ്ങൾ പുതിയ ആളാണെങ്കിൽ പണത്തിൻ്റെ അടിസ്ഥാനകാര്യങ്ങളിൽ നിന്ന് തുടങ്ങാൻ ഞങ്ങൾ നിർദ്ദേശിക്കുന്നു!",
      quizQuestion: "സാമ്പത്തിക യാത്ര തുടങ്ങാൻ ഏറ്റവും നല്ല മാർഗം ഏതാണ്?",
      options: ["ചെലവുകൾ അവഗണിക്കുക", "അടിസ്ഥാന കാര്യങ്ങൾ പഠിക്കുക", "ചൂതാട്ടം", "ഒന്നും ചെയ്യാതിരിക്കുക"]
    }
  },
  ta: {
    categories: [
      { 
        id: 'money-basics', 
        title: '1. பணத்தின் அடிப்படைகள்', 
        description: 'வங்கிகள் அல்லது செயலிகளைப் பயன்படுத்தும் முன் பணத்தைப் பற்றி புரிந்து கொள்ளுங்கள். வருமானம், வரவு செலவுத் திட்டம் மற்றும் பணவீக்கம்.',
        topics: {
          level1: "1.1 வருமானம் மற்றும் செலவுகள்",
          level2: "1.2 பட்ஜெட் மற்றும் பணப்புழக்கம்",
          level3: "1.3 பணவீக்கம் மற்றும் பணத்தின் மதிப்பு"
        }
      },
      { 
        id: 'banking', 
        title: '2. வங்கிச் சேவை', 
        description: 'வங்கிகளை நம்பிக்கையுடன் பயன்படுத்துங்கள். கணக்குகள், வங்கி வணிக மாதிரிகள் மற்றும் தினசரி செயல்பாடுகள்.',
        topics: {
          level1: "2.1 வங்கி கணக்குகள் (சேமிப்பு, நடப்பு)",
          level2: "2.2 வங்கிகள் எப்படி சம்பாதிக்கின்றன",
          level3: "2.3 வங்கி செயல்பாடுகள் (ஏடிஎம், கேஒய்சி)"
        }
      },
      { 
        id: 'digital-payments', 
        title: '3. டிஜிட்டல் கொடுப்பனவுகள்', 
        description: 'பயமின்றி தினசரி டிஜிட்டல் பணப் பயன்பாடு. UPI மற்றும் பாதுகாப்பான பரிமாற்றங்களைக் கற்றுக்கொள்ளுங்கள்.',
        topics: {
          level1: "3.1 UPI எப்படி வேலை செய்கிறது",
          level2: "3.2 இடமாற்றங்கள் மற்றும் கொடுப்பனவுகள்",
          level3: "3.3 தோல்வியுற்ற கொடுப்பனவுகள்"
        }
      },
      { 
        id: 'saving-investing', 
        title: '4. சேமிப்பு & முதலீடு', 
        description: 'பணத்தைப் பாதுகாப்பாக வளர்க்கவும். கூட்டு வட்டி, இடர் மேலாண்மை மற்றும் சேமிப்பு கருவிகள்.',
        topics: {
          level1: "4.1 சேமிப்பு விருப்பங்கள்",
          level2: "4.2 வருமானம் மற்றும் கூட்டு வட்டி",
          level3: "4.3 ஆபத்து அடிப்படைகள்"
        }
      },
      { 
        id: 'loans-credit', 
        title: '5. கடன்கள் & கிரெடிட்', 
        description: 'புத்திசாலித்தனமாக கடன் வாங்குங்கள், கடன் வலையில் விழாதீர்கள். EMIகள், கடன் வகைகள் மற்றும் கிரெடிட் மதிப்பெண்கள்.',
        topics: {
          level1: "5.1 கடன் வகைகள்",
          level2: "5.2 EMI மற்றும் வட்டி",
          level3: "5.3 கிரெடிட் கார்டுகள்"
        }
      },
      { 
        id: 'safety-protection', 
        title: '6. பாதுகாப்பு', 
        description: 'உங்கள் பணத்தையும் எதிர்காலத்தையும் பாதுகாக்கவும். மோசடிகள், வரிகள் மற்றும் காப்பீட்டு அடிப்படைகள்.',
        topics: {
          level1: "6.1 ஆன்லைன் பாதுகாப்பு",
          level2: "6.2 வரி அடிப்படைகள்",
          level3: "6.3 காப்பீடு (ஆரோக்கியம் vs வாழ்க்கை)"
        }
      }
    ],
    welcomeLesson: {
      title: "FINQUEST க்கு வரவேற்கிறோம்",
      content: "FINQUEST உங்கள் நிதி சுதந்திரத்திற்கான தனிப்பட்ட வழிகாட்டியாகும். உங்கள் தற்போதைய தேவைகளுக்கு ஏற்ற பாதையைத் தேர்ந்தெடுப்பதன் மூலம் தொடங்கவும். நீங்கள் புதியவராக இருந்தால் பணத்தின் அடிப்படைகளுடன் தொடங்க பரிந்துரைக்கிறோம்!",
      quizQuestion: "உங்கள் நிதிப் பயணத்தைத் தொடங்க சிறந்த வழி எது?",
      options: ["செலவுகளைப் புறக்கணித்தல்", "அடிப்படைகளைக் கற்றுக்கொள்வது", "சூதாட்டம்", "ஒன்றும் செய்யாமல் இருப்பது"]
    }
  },
  hi: {
    categories: [
      { 
        id: 'money-basics', 
        title: '1. पैसे की बुनियादी बातें', 
        description: 'बैंकों या ऐप्स को छूने से पहले पैसे को समझें। आय, बजट और मुद्रास्फीति।',
        topics: {
          level1: "1.1 आय और व्यय (आय के प्रकार बनाम जरूरतें)",
          level2: "1.2 बजट और नकदी प्रवाह",
          level3: "1.3 मुद्रास्फीति और पैसे का मूल्य"
        }
      },
      { 
        id: 'banking', 
        title: '2. बैंकिंग', 
        description: 'विश्वास के साथ बैंकों का उपयोग करें। खाते, बैंक व्यवसाय मॉडल और दैनिक संचालन।',
        topics: {
          level1: "2.1 बैंक खाते (बचत, चालू)",
          level2: "2.2 बैंक पैसे कैसे कमाते हैं",
          level3: "2.3 बैंकिंग संचालन (एटीएम, केवाईसी)"
        }
      },
      { 
        id: 'digital-payments', 
        title: '3. डिजिटल भुगतान', 
        description: 'बिना किसी डर के दैनिक डिजिटल पैसे का उपयोग। UPI और सुरक्षित ट्रांसफर में महारत हासिल करें।',
        topics: {
          level1: "3.1 UPI कैसे काम करता है",
          level2: "3.2 स्थानांतरण और भुगतान",
          level3: "3.3 विफल भुगतान और रिफंड"
        }
      },
      { 
        id: 'saving-investing', 
        title: '4. बचत और निवेश', 
        description: 'पैसे को सुरक्षित रूप से बढ़ाएं। कंपाउंडिंग, जोखिम प्रबंधन और बचत उपकरण।',
        topics: {
          level1: "4.1 बचत विकल्प (FD, RD)",
          level2: "4.2 रिटर्न और कंपाउंडिंग",
          level3: "4.3 जोखिम की बुनियादी बातें"
        }
      },
      { 
        id: 'loans-credit', 
        title: '5. ऋण और क्रेडिट', 
        description: 'समझदारी से उधार लें, कर्ज के जाल से बचें। ईएमआई, ऋण के प्रकार और क्रेडिट स्कोर।',
        topics: {
          level1: "5.1 ऋण के प्रकार",
          level2: "5.2 ईएमआई और ब्याज",
          level3: "5.3 क्रेडिट कार्ड और सिबिल"
        }
      },
      { 
        id: 'safety-protection', 
        title: '6. सुरक्षा और संरक्षण', 
        description: 'अपने पैसे और भविष्य की रक्षा करें। घोटाले, कर और बीमा की बुनियादी बातें।',
        topics: {
          level1: "6.1 ऑनलाइन सुरक्षा",
          level2: "6.2 कर की बुनियादी बातें",
          level3: "6.3 बीमा (स्वास्थ्य बनाम जीवन)"
        }
      }
    ],
    welcomeLesson: {
      title: "FINQUEST में आपका स्वागत है",
      content: "FINQUEST वित्तीय स्वतंत्रता के लिए आपका व्यक्तिगत मार्गदर्शक है। अपनी वर्तमान जरूरतों के अनुकूल रास्ता चुनकर शुरुआत करें। यदि आप नए हैं तो हम 'पैसे की बुनियादी बातें' से शुरुआत करने का सुझाव देते हैं!",
      quizQuestion: "अपनी वित्तीय यात्रा शुरू करने का सबसे अच्छा तरीका क्या है?",
      options: ["खर्चों को नजरअंदाज करना", "बुनियादी बातें सीखना", "जुआ खेलना", "कुछ नहीं करना"]
    }
  }
};

interface BaseCategory {
  id: string;
  icon: string;
  difficulty: 'Easy' | 'Medium' | 'Hard';
}

const BASE_CATEGORIES: BaseCategory[] = [
  { id: 'money-basics', icon: '💸', difficulty: 'Easy' },
  { id: 'banking', icon: '🏛️', difficulty: 'Medium' },
  { id: 'digital-payments', icon: '📱', difficulty: 'Easy' },
  { id: 'saving-investing', icon: '📈', difficulty: 'Medium' },
  { id: 'loans-credit', icon: '💳', difficulty: 'Hard' },
  { id: 'safety-protection', icon: '🛡️', difficulty: 'Hard' }
];

const TRANSLATIONS = {
  en: {
    start: 'PROCEED',
    login: 'LOGIN',
    signup: 'SIGN UP',
    enter: 'ENTER',
    join: 'JOIN',
    username: 'USERNAME',
    password: 'PASSWORD',
    paths: 'PATHS',
    profile: 'PROFILE',
    store: 'STORE',
    choosePath: 'CHOOSE YOUR PATH',
    pathDesc: 'Master all paths to unlock the Rank Up Exam and advance to the next level.',
    progress: 'RANK PROGRESS',
    completed: 'MASTERED',
    mastered: 'MASTERED',
    locked: 'LOCKED',
    begin: 'BEGIN TRIAL',
    retreat: 'RETREAT',
    submit: 'SUBMIT ANSWER',
    continue: 'CONTINUE JOURNEY',
    backRoadmap: 'BACK TO ROADMAP',
    reread: 'RE-READ LESSON',
    passed: 'TRIAL PASSED',
    failed: 'TRIAL FAILED',
    xpGranted: 'XP GRANTED • KNOWLEDGE UNLOCKED',
    tryAgain: 'RETURN TO THE SCROLL AND TRY AGAIN',
    level: 'RANK',
    settings: 'SETTINGS',
    language: 'LANGUAGE',
    notifications: 'NOTIFICATIONS',
    account: 'ACCOUNT',
    resources: 'SCROLLS OF WISDOM (RESOURCES)',
    signupCta: "Don't have an account?",
    loginCta: "Already a member?",
    welcome: "Welcome to FINQUEST",
    newHere: "NEW HERE?",
    createAccount: "CREATE ACCOUNT",
    backToLogin: "BACK TO LOGIN",
    pathBonus: "PATH COMPLETED! +500 XP",
    rankUpReady: "RANK UP EXAM UNLOCKED",
    takeExam: "TAKE EXAM",
    examDesc: "Prove your mastery to advance to the next rank.",
    buy: "BUY",
    equip: "EQUIP",
    equipped: "EQUIPPED",
    owned: "OWNED",
    cost: "COST",
    commandCenter: "COMMAND CENTER",
    easy: 'EASY', medium: 'MEDIUM', hard: 'HARD'
  },
  te: { start: 'కొనసాగండి', login: 'లాగిన్', signup: 'సైన్ అప్', enter: 'ప్రవేశించండి', join: 'చేరండి', username: 'యూజర్ పేరు', password: 'పాస్‌వర్డ్', paths: 'మార్గాలు', profile: 'ప్రొఫైల్', store: 'స్టోర్', choosePath: 'మీ మార్గాన్ని ఎంచుకోండి', pathDesc: 'అన్ని మార్గాలను పూర్తి చేసి తదుపరి ర్యాంక్‌కు వెళ్లండి.', progress: 'ర్యాంక్ పురోగతి', completed: 'పూర్తయింది', mastered: 'నైపుణ్యం', locked: 'లాక్ చేయబడింది', begin: 'ప్రారంభించండి', retreat: 'వెనుకకు', submit: 'సమర్పించండి', continue: 'కొనసాగించండి', backRoadmap: 'తిరిగి వెళ్ళు', reread: 'మళ్ళీ చదవండి', passed: 'ఉత్తీర్ణులయ్యారు', failed: 'విఫలమయ్యారు', xpGranted: 'XP పొందారు', tryAgain: 'మళ్ళీ ప్రయత్నించండి', level: 'ర్యాంక్', settings: 'అమరికలు', language: 'భాష', notifications: 'నోటిఫికేషన్లు', account: 'ఖాతా', resources: 'వనరులు', signupCta: "ఖాతా లేదా?", loginCta: "సభ్యులేనా?", welcome: "స్వాగతం", newHere: "కొత్తవారా?", createAccount: "ఖాతా సృష్టించు", backToLogin: "లాగిన్", pathBonus: "పూర్తయింది! +500 XP", rankUpReady: "ర్యాంక్ పరీక్ష సిద్ధంగా ఉంది", takeExam: "పరీక్ష రాయండి", examDesc: "మీ నైపుణ్యాన్ని నిరూపించుకోండి.", buy: "కొనుగోలు", equip: "ధరించండి", equipped: "ధరించారు", owned: "స్వంతం", cost: "ధర", commandCenter: "కమాండ్ సెంటర్", easy: 'సులభం', medium: 'మధ్యస్థం', hard: 'కఠినం' },
  kn: { start: 'ಮುಂದುವರಿಯಿರಿ', login: 'ಲಾಗಿನ್', signup: 'ಸೈನ್ ಅಪ್', enter: 'ಪ್ರವೇಶಿಸಿ', join: 'ಸೇರಿ', username: 'ಬಳಕೆದಾರ ಹೆಸರು', password: 'ಪಾಸ್‌ವರ್ಡ್', paths: 'ಮಾರ್ಗಗಳು', profile: 'ಪ್ರೊಫైಲ್', store: 'ಅಂಗಡಿ', choosePath: 'ನಿಮ್ಮ ದಾರಿಯನ್ನು ಆರಿಸಿ', pathDesc: 'ಮುಂದಿನ ಹಂತಕ್ಕೆ ಹೋಗಲು ಎಲ್ಲಾ ಮಾರ್ಗಗಳನ್ನು ಪೂರ್ಣಗೊಳಿಸಿ.', progress: 'ಶ್ರೇಣಿ ಪ್ರಗತಿ', completed: 'ಪೂರ್ಣಗೊಂಡಿದೆ', mastered: 'ಕರಗತವಾಗಿದೆ', locked: 'ಲಾಕ್ ಆಗಿದೆ', begin: 'ಪ್ರಾರಂಭಿಸಿ', retreat: 'ಹಿಂದೆ', submit: 'ಸಲ್ಲಿಸಿ', continue: 'ಮುಂದುವರಿಸಿ', backRoadmap: 'ಹಿಂದಕ್ಕೆ', reread: 'ಮತ್ತೊಮ್ಮೆ ಓದಿ', passed: 'ಪಾಸಾಗಿದೆ', failed: 'ವಿಫಲವಾಗಿದೆ', xpGranted: 'XP ಲಭಿಸಿದೆ', tryAgain: 'ಮತ್ತೊಮ್ಮೆ ಪ್ರಯತ್ನಿಸಿ', level: 'ಹಂತ', settings: 'ಸೆಟ್ಟಿಂಗ್‌ಗಳು', language: 'ಭಾಷೆ', notifications: 'ಸೂಚನೆಗಳು', account: 'ಖಾತೆ', resources: 'ಸಂಪನ್ಮೂಲಗಳು', signupCta: "ಖಾತೆ ಇಲ್ಲವೇ?", loginCta: "ಈಗಾಗಲೇ ಸದಸ್ಯರೇ?", welcome: "ಸ್ವಾಗತ", newHere: "ಹೊಸಬರೇ?", createAccount: "ಖಾತೆ ತೆರೆಯಿರಿ", backToLogin: "ಲಾಗಿನ್", pathBonus: "ಪೂರ್ಣಗೊಂಡಿದೆ! +500 XP", rankUpReady: "ಶ್ರೇಣಿ ಪರೀಕ್ಷೆ ಅನ್‌ಲಾಕ್ ಆಗಿದೆ", takeExam: "ಪರೀಕ್ಷೆ ತೆಗೆದುಕೊ", examDesc: "ಮುಂದಿನ ಹಂತಕ್ಕೆ ಹೋಗಲು ಪರೀಕ್ಷೆ ಬರೆಯಿರಿ.", buy: "ಖರೀದಿಸಿ", equip: "ಬಳಸಿ", equipped: "ಬಳಸಲಾಗಿದೆ", owned: "ಸ್ವಂತ", cost: "ಬೆಲೆ", commandCenter: "ಕಮಾಂಡ್ ಸೆಂಟರ್", easy: 'ಸುಲಭ', medium: 'ಮಧ್ಯಮ', hard: 'ಕಠಿಣ' },
  ml: { start: 'തുടരുക', login: 'ലോഗിൻ', signup: 'സൈൻ അപ്പ്', enter: 'പ്രവേശിക്കുക', join: 'ചേരുക', username: 'ഉപയോക്തൃനാമം', password: 'പാസ്‌വേഡ്', paths: 'വഴികൾ', profile: 'പ്രൊഫൈൽ', store: 'സ്റ്റോർ', choosePath: 'നിങ്ങളുടെ വഴി തിരഞ്ഞെടുക്കുക', pathDesc: 'അടുത്ത റാങ്കിലേക്ക് പോകാൻ എല്ലാ വഴികളും പൂർത്തിയാക്കുക.', progress: 'റാങ്ക് പുരോഗതി', completed: 'പൂർത്തിയായി', mastered: 'നേടി', locked: 'ലോക്ക് ചെയ്തു', begin: 'തുടങ്ങുക', retreat: 'പിന്നോട്ട്', submit: 'സമർപ്പിക്കുക', continue: 'തുടരുക', backRoadmap: 'തിരികെ', reread: 'വീണ്ടും വായിക്കുക', passed: 'വിജയിച്ചു', failed: 'പരാജയപ്പെട്ടു', xpGranted: 'XP ലഭിച്ചു', tryAgain: 'വീണ്ടും ശ്രമിക്കുക', level: 'റാങ്ക്', settings: 'ക്രമീകരണങ്ങൾ', language: 'ഭാഷ', notifications: 'അറിയിപ്പുകൾ', account: 'അക്കൗണ്ട്', resources: 'വിഭവങ്ങൾ', signupCta: "അക്കൗണ്ട് ഇല്ലേ?", loginCta: "അംഗമാണോ?", welcome: "സ്വാഗതം", newHere: "പുതിയ ആളാണോ?", createAccount: "അക്കൗണ്ട് ഉണ്ടാക്കുക", backToLogin: "ലോഗിൻ", pathBonus: "പൂർത്തിയായി! +500 XP", rankUpReady: "റാങ്ക് പരീക്ഷ തയ്യാറാണ്", takeExam: "പരീക്ഷ എഴുതുക", examDesc: "അടുത്ത റാങ്കിലേക്ക് പോകാൻ യോഗ്യത തെളിയിക്കുക.", buy: "വാങ്ങുക", equip: "ഉപയോഗിക്കുക", equipped: "ഉപയോഗിക്കുന്നു", owned: "സ്വന്തം", cost: "വില", commandCenter: "കമാൻഡ് സെന്റർ", easy: 'ലളിതം', medium: 'ഇടത്തരം', hard: 'കഠിനം' },
  ta: { start: 'தொடரவும்', login: 'உள்நுழைக', signup: 'பதிவு', enter: 'உள்ளிடவும்', join: 'சேரவும்', username: 'பயனர்பெயர்', password: 'கடவுச்சொல்', paths: 'வழிகள்', profile: 'சுயவிவரம்', store: 'கடை', choosePath: 'உங்கள் வழியைத் தேர்வுசெய்க', pathDesc: 'அடுத்த நிலைக்குச் செல்ல அனைத்து வழிகளையும் முடிக்கவும்.', progress: 'தர முன்னேற்றம்', completed: 'முடிந்தது', mastered: 'தேர்ச்சி', locked: 'பூட்டப்பட்டது', begin: 'தொடங்கவும்', retreat: 'பின்வாங்கு', submit: 'சமர்ப்பிக்கவும்', continue: 'தொடரவும்', backRoadmap: 'திரும்பிச் செல்', reread: 'மீண்டும் படி', passed: 'தேர்ச்சி', failed: 'தோல்வி', xpGranted: 'XP வழங்கப்பட்டது', tryAgain: 'மீண்டும் முயற்சிக்கவும்', level: 'தரம்', settings: 'அமைப்புகள்', language: 'மொழி', notifications: 'அறிவிப்புகள்', account: 'கணக்கு', resources: 'வளங்கள்', signupCta: "கணக்கு இல்லையா?", loginCta: "ஏற்கனவே உறுப்பினரா?", welcome: "வரவேற்பு", newHere: "புதியவரா?", createAccount: "கணக்கை உருவாக்கு", backToLogin: "உள்நுழைக", pathBonus: "முடிந்தது! +500 XP", rankUpReady: "தேர்வு தயார்", takeExam: "தேர்வை எழுதுங்கள்", examDesc: "அடுத்த நிலைக்குச் செல்ல உங்கள் திறமையை நிரூபிக்கவும்.", buy: "வாங்க", equip: "பயன்படுத்து", equipped: "பயன்பாட்டில்", owned: "சொந்தம்", cost: "விலை", commandCenter: "கட்டளை மையம்", easy: 'எளிது', medium: 'நடுத்தரம்', hard: 'கடினம்' },
  hi: { start: 'आगे बढ़ें', login: 'लॉग इन', signup: 'साइन अप', enter: 'प्रवेश करें', join: 'शामिल हों', username: 'यूज़रनेम', password: 'पासवर्ड', paths: 'रास्ते', profile: 'प्रोफ़ाइल', store: 'स्टोर', choosePath: 'अपना रास्ता चुनें', pathDesc: 'अगले रैंक पर जाने के लिए सभी रास्ते पूरे करें।', progress: 'रैंक प्रगति', completed: 'पूरा हुआ', mastered: 'महारत हासिल', locked: 'बंद है', begin: 'ट्रायल शुरू करें', retreat: 'पीछे हटें', submit: 'उत्तर जमा करें', continue: 'जारी रखें', backRoadmap: 'वापस जाएं', reread: 'फिर से पढ़ें', passed: 'पास', failed: 'फेल', xpGranted: 'XP मिला', tryAgain: 'फिर से कोशिश करें', level: 'रैंक', settings: 'सेटिंग्स', language: 'भाषा', notifications: 'सूचनाएं', account: 'खाता', resources: 'संसाधन', signupCta: "खाता नहीं है?", loginCta: "पहले से सदस्य हैं?", welcome: "स्वागत है", newHere: "नए हैं?", createAccount: "खाता बनाएं", backToLogin: "लॉग इन", pathBonus: "पथ पूरा हुआ! +500 XP", rankUpReady: "रैंक परीक्षा अनलॉक", takeExam: "परीक्षा दें", examDesc: "अगले रैंक पर जाने के लिए परीक्षा पास करें।", buy: "खरीदें", equip: "इस्तेमाल करें", equipped: "इस्तेमाल में", owned: "स्वामित्व", cost: "लागत", commandCenter: "कमांड सेंटर", easy: 'आसान', medium: 'मध्यम', hard: 'कठिन' }
};

const GeminiStar: React.FC<{ style: React.CSSProperties }> = ({ style }) => (
  <svg 
    viewBox="0 0 24 24" 
    className="gemini-star-bg" 
    style={style}
  >
    <path d="M12 2L14.5 9.5L22 12L14.5 14.5L12 22L9.5 14.5L2 12L9.5 9.5L12 2Z"/>
  </svg>
);

type ShapeElement = {
  id: string;
  type: string;
  color: string;
  size: number;
  left: number;
  top: number;
  duration: number;
  delay: number;
  driftX: number;
  driftY: number;
  isStar: false;
};

type StarElement = {
  id: string;
  size: number;
  left: number;
  top: number;
  duration: number;
  delay: number;
  isStar: true;
};

type BackgroundElement = ShapeElement | StarElement;

const GeometricBackground = memo(() => {
  const elements = useMemo(() => {
    const types = ['circle', 'square', 'triangle', 'rectangle'];
    const colors = ['bg-white', 'bg-[#90EE90]', 'bg-[#FFD700]', 'bg-black/10'];
    
    const shapes: ShapeElement[] = Array.from({ length: 24 }).map((_, i) => ({
      id: `shape-${i}`,
      type: types[Math.floor(Math.random() * types.length)],
      color: colors[Math.floor(Math.random() * colors.length)],
      size: 40 + Math.random() * 90,
      left: Math.random() * 100,
      top: Math.random() * 100,
      duration: 10 + Math.random() * 25,
      delay: -Math.random() * 20,
      driftX: (Math.random() - 0.5) * 350,
      driftY: (Math.random() - 0.5) * 350,
      isStar: false
    }));

    const stars: StarElement[] = Array.from({ length: 50 }).map((_, i) => ({
      id: `star-${i}`,
      size: 12 + Math.random() * 22,
      left: Math.random() * 100,
      top: Math.random() * 100,
      duration: 2 + Math.random() * 4,
      delay: -Math.random() * 10,
      isStar: true
    }));

    return [...shapes, ...stars] as BackgroundElement[];
  }, []);

  return (
    <div id="bg-layer">
      <div className="bg-grid-animation"></div>
      {elements.map((el) => {
        if (el.isStar) {
          return (
            <GeminiStar 
              key={el.id}
              style={{
                left: `${el.left}%`,
                top: `${el.top}%`,
                width: `${el.size}px`,
                height: `${el.size}px`,
                animationDuration: `${el.duration}s`,
                animationDelay: `${el.delay}s`
              }}
            />
          );
        }
        
        return (
          <div 
            key={el.id}
            className={`floating-shape ${el.color} ${el.type === 'circle' ? 'rounded-full' : ''}`}
            style={{
              left: `${el.left}%`,
              top: `${el.top}%`,
              width: el.type === 'rectangle' ? `${el.size * 2}px` : `${el.size}px`,
              height: `${el.size}px`,
              animationDuration: `${el.duration}s`,
              animationDelay: `${el.delay}s`,
              clipPath: el.type === 'triangle' ? 'polygon(50% 0%, 0% 100%, 100% 100%)' : 'none',
              '--drift-x': `${el.driftX}px`,
              '--drift-y': `${el.driftY}px`
            } as React.CSSProperties}
          />
        );
      })}
    </div>
  );
});

// Explicit Rank Hierarchy
const RANK_ORDER = [
  'Beginner I', 'Beginner II', 'Beginner III',
  'Intermediate I', 'Intermediate II', 'Intermediate III',
  'Expert I', 'Expert II', 'Expert III',
  'Grandmaster'
];

type FullUserProgress = UserProgress & { categoryProgress: Record<string, number>, completedRoadmapTitles?: string[] };

const DEFAULT_PROGRESS: FullUserProgress = { 
  completedLessonIds: [], 
  xp: 0, 
  badges: [], 
  rankIndex: 0, 
  completedCategoriesForCurrentRank: [], 
  completedRoadmapTitles: [], 
  categoryProgress: {}, 
  language: 'en', 
  inventory: ['banner_classic'], 
  equippedBanner: 'banner_classic' 
};

const App: React.FC = () => {
  const [view, setView] = useState<ViewState>('START');
  const [isLoginMode, setIsLoginMode] = useState(true);
  const [username, setUsername] = useState('');
  const [password, setPassword] = useState('');
  const [displayPassword, setDisplayPassword] = useState('');
  const [maskTimeout, setMaskTimeout] = useState<any | null>(null);

  const [roadmap, setRoadmap] = useState<Level[]>([]);
  const [currentCategory, setCurrentCategory] = useState('');
  const [activeLesson, setActiveLesson] = useState<Lesson | null>(null);
  
  const [progress, setProgress] = useState<FullUserProgress>(DEFAULT_PROGRESS);

  const [selectedOption, setSelectedOption] = useState<number | null>(null);
  const [quizSuccess, setQuizSuccess] = useState(false);
  const [earnedXp, setEarnedXp] = useState(0);
  const [isMuted, setIsMuted] = useState(false);
  const [audioInitialized, setAudioInitialized] = useState(false);

  // Rank Exam State
  const [rankExamQuestions, setRankExamQuestions] = useState<Quiz[]>([]);
  const [currentExamQuestionIndex, setCurrentExamQuestionIndex] = useState(0);
  const [examScore, setExamScore] = useState(0);

  const t = TRANSLATIONS[progress.language];
  const audioRef = useRef<HTMLAudioElement | null>(null);
  const currentRankName = RANK_ORDER[progress.rankIndex];

  // Derive categories
  const categories = useMemo(() => {
    const localized = LOCALIZED_CONTENT[progress.language].categories;
    return BASE_CATEGORIES.map(base => {
      const loc = localized.find(l => l.id === base.id);
      return { 
        ...base, 
        title: loc?.title || base.id, 
        description: loc?.description || '',
        topics: loc?.topics || { level1: '', level2: '', level3: '' }
      };
    });
  }, [progress.language]);

  const defaultLevels = useMemo<Level[]>(() => {
    const w = LOCALIZED_CONTENT[progress.language].welcomeLesson;
    return [{
      id: 1,
      title: "Introduction",
      lessons: [{
          id: "l1", title: w.title, content: w.content, unlocked: true, resources: [],
          quiz: { question: w.quizQuestion, options: w.options, correct: 1 }
        }]
    }];
  }, [progress.language]);

  useEffect(() => {
    if (!currentCategory) setRoadmap(defaultLevels);
  }, [defaultLevels, currentCategory]);

  useEffect(() => {
    const audio = new Audio('https://cdn.pixabay.com/audio/2022/05/27/audio_1808fbf07a.mp3'); 
    audio.loop = true;
    audio.volume = 0.12;
    audioRef.current = audio;
    return () => { if (audioRef.current) { audioRef.current.pause(); audioRef.current = null; } };
  }, []);

  useEffect(() => {
    if (audioRef.current) audioRef.current.muted = isMuted;
  }, [isMuted]);

  // Save Progress to LocalStorage whenever it changes, if user is logged in
  useEffect(() => {
    if (username.trim() && view !== 'START' && view !== 'AUTH') {
      const storageKey = `finquest_user_${username.trim()}`;
      localStorage.setItem(storageKey, JSON.stringify(progress));
    }
  }, [progress, username, view]);

  const handleStartMusic = () => {
    if (audioRef.current && !audioInitialized) {
      audioRef.current.play().then(() => setAudioInitialized(true)).catch(e => console.error(e));
    }
    setView('AUTH');
  };

  const goToPaths = () => {
    setView('ONBOARDING');
    setCurrentCategory('');
    setRoadmap(defaultLevels);
    setActiveLesson(null);
    setSelectedOption(null);
  };
  
  const goToHub = () => {
     setView('HOME');
  }

  // Rank Progress Calculation (Paths Completed / Total Paths)
  const rankCompletionPercentage = Math.round((progress.completedCategoriesForCurrentRank.length / categories.length) * 100);
  const isReadyForRankUp = rankCompletionPercentage === 100 && progress.rankIndex < RANK_ORDER.length - 1;

  const roadmapProgress = useMemo(() => {
    if (!roadmap.length) return 0;
    if (roadmap[0].id === 1 && roadmap[0].lessons[0].id === 'l1') return 0;
    const allLessons = roadmap.flatMap(l => l.lessons);
    if (allLessons.length === 0) return 0;
    const completed = allLessons.filter(l => progress.completedLessonIds.includes(l.id)).length;
    return Math.round((completed / allLessons.length) * 100);
  }, [roadmap, progress.completedLessonIds]);

  const isCurrentRoadmapComplete = roadmapProgress === 100 && !!currentCategory;

  // Mark Path as Complete for Current Rank
  useEffect(() => {
    if (isCurrentRoadmapComplete && currentCategory && !progress.completedCategoriesForCurrentRank.includes(currentCategory)) {
      setProgress(prev => ({
        ...prev,
        xp: prev.xp + 500,
        completedCategoriesForCurrentRank: [...prev.completedCategoriesForCurrentRank, currentCategory],
        categoryProgress: { ...prev.categoryProgress, [currentCategory]: 100 }
      }));
    }
  }, [isCurrentRoadmapComplete, currentCategory]);

  const handleAuth = (e: React.FormEvent) => {
    e.preventDefault();
    if (username.trim() && password.trim()) {
      const storageKey = `finquest_user_${username.trim()}`;
      const savedData = localStorage.getItem(storageKey);
      
      if (savedData) {
        try {
          const parsed = JSON.parse(savedData);
          // Merge with default to ensure all required fields are present
          setProgress({ ...DEFAULT_PROGRESS, ...parsed });
        } catch (e) {
          console.error("Failed to load save", e);
          setProgress(DEFAULT_PROGRESS);
        }
      } else {
        // New user
        setProgress(DEFAULT_PROGRESS);
      }
      setView('HOME');
    }
  };

  const handleLogout = () => {
    setUsername('');
    setPassword('');
    setProgress(DEFAULT_PROGRESS);
    setView('START');
  };

  const handleSelectCategory = async (cat: Category) => {
    // If mastered for this rank, don't allow replay (or allow review mode - here we allow review)
    setCurrentCategory(cat.title);
    setView('LOADING');
    const minLoadTime = new Promise(resolve => setTimeout(resolve, 2000));
    
    try {
      const [customRoadmap] = await Promise.all([
        generateCustomRoadmap(cat.title, cat.difficulty, cat.topics, progress.language, currentRankName),
        minLoadTime
      ]);
      
      if (customRoadmap && customRoadmap.length > 0) {
        // BUG FIX: Ensure lesson IDs are unique to the current rank + category
        // This prevents the system from thinking a lesson is complete just because ID matches "lesson_1" from a previous rank
        const uniqueRoadmap = customRoadmap.map(level => ({
          ...level,
          lessons: level.lessons.map(lesson => ({
            ...lesson,
            id: `${progress.rankIndex}_${cat.id}_${lesson.id}` 
          }))
        }));
        setRoadmap(uniqueRoadmap);
      }
      setView('ROADMAP');
    } catch (e) {
      goToPaths();
    }
  };

  const handleFinishQuiz = () => {
    if (activeLesson && selectedOption === activeLesson.quiz.correct) {
      setQuizSuccess(true);
      if (!progress.completedLessonIds.includes(activeLesson.id)) {
        let baseXp = 50 + (progress.rankIndex * 20); // Scale XP by Rank
        setEarnedXp(baseXp);
        
        const allLessons = roadmap.flatMap(l => l.lessons);
        const newlyCompletedCount = allLessons.filter(l => progress.completedLessonIds.includes(l.id) || l.id === activeLesson.id).length;
        const newCatProgress = Math.round((newlyCompletedCount / allLessons.length) * 100);

        setProgress(prev => ({
          ...prev,
          completedLessonIds: [...prev.completedLessonIds, activeLesson.id],
          xp: prev.xp + baseXp,
          categoryProgress: { ...prev.categoryProgress, [currentCategory]: newCatProgress }
        }));
      } else {
        setEarnedXp(0);
      }
    } else {
      setQuizSuccess(false);
      setEarnedXp(0);
    }
    setView('QUIZ_SUMMARY');
  };

  const handleBuyItem = (item: StoreItem) => {
    if (progress.xp >= item.cost && !progress.inventory.includes(item.id)) {
      setProgress(prev => ({
        ...prev,
        xp: prev.xp - item.cost,
        inventory: [...(prev.inventory || []), item.id]
      }));
    }
  };

  const handleEquipItem = (item: StoreItem) => {
    if (item.type === 'banner') {
      setProgress(prev => ({
        ...prev,
        equippedBanner: item.id
      }));
    }
  };

  const startRankExam = async () => {
    setView('LOADING');
    try {
      const questions = await generateRankExam(currentRankName, progress.language);
      if (questions.length > 0) {
        setRankExamQuestions(questions);
        setCurrentExamQuestionIndex(0);
        setExamScore(0);
        setView('RANK_EXAM');
      } else {
        // Fallback or error
        goToPaths();
      }
    } catch(e) {
      goToPaths();
    }
  };

  const submitExamAnswer = (selectedIndex: number) => {
    const currentQ = rankExamQuestions[currentExamQuestionIndex];
    if (selectedIndex === currentQ.correct) {
      setExamScore(s => s + 1);
    }

    if (currentExamQuestionIndex < rankExamQuestions.length - 1) {
      setCurrentExamQuestionIndex(i => i + 1);
    } else {
      // Exam Finished
      // Check if score is sufficient (e.g., 3/5)
      const finalScore = selectedIndex === currentQ.correct ? examScore + 1 : examScore;
      if (finalScore >= 3) {
        // RANK UP!
        const nextRankIdx = progress.rankIndex + 1;
        setProgress(prev => ({
          ...prev,
          rankIndex: nextRankIdx,
          completedCategoriesForCurrentRank: [], // Reset for new rank
          categoryProgress: {}, // Reset progress bars
          badges: [...prev.badges, currentRankName], // Award old rank as badge
          xp: prev.xp + 1000
        }));
        setView('RANK_SUCCESS');
      } else {
        // Failed
        setView('QUIZ_SUMMARY'); // Reuse summary or make custom fail screen
        setQuizSuccess(false);
        setEarnedXp(0);
      }
    }
  };

  const NavHeader = () => (
    <div className="flex justify-between items-center mb-12 border-b-2 border-black pb-4">
      <div className="flex items-center gap-4">
        <button onClick={goToHub} className="text-2xl font-arcane title-main hover:scale-105 transition-transform active:scale-95">FINQUEST</button>
        <div className="hidden lg:flex items-center gap-2">
          <div className="bg-black text-[#90EE90] px-3 py-1 border-2 border-black neo-shadow font-black uppercase text-xs">
             {t.level}: {currentRankName}
          </div>
          <div className="flex flex-col gap-0.5">
            <div className="h-6 w-32 progress-bar-container">
              <div className="progress-bar-inner" style={{ width: `${rankCompletionPercentage}%` }}></div>
            </div>
            <span className="text-[10px] font-black uppercase text-right leading-none opacity-60">
              {progress.completedCategoriesForCurrentRank.length}/{categories.length} Paths
            </span>
          </div>
        </div>
      </div>
      <div className="flex gap-3 items-center">
        <button onClick={() => setIsMuted(!isMuted)} className="p-2 border-2 border-black neo-shadow bg-white hover:bg-gray-100 transition-colors">{isMuted ? '🔈' : '🔊'}</button>
        <NeoButton variant="secondary" onClick={() => setView('STORE')} className="text-xs py-1.5 px-3">{t.store}</NeoButton>
        <NeoButton variant="secondary" onClick={() => setView('ONBOARDING')} className="text-xs py-1.5 px-3">{t.paths}</NeoButton>
        <NeoButton variant="secondary" onClick={() => setView('SETTINGS')} className="text-xs py-1.5 px-3">{t.settings}</NeoButton>
      </div>
    </div>
  );

  const renderContent = () => {
    switch (view) {
      case 'START':
        return (
          <div className="min-h-screen flex flex-col items-center justify-center p-4 fade-in">
            <h1 className="text-7xl md:text-9xl font-arcane mb-4 text-center title-main uppercase">FINQUEST</h1>
            <p className="text-xl md:text-2xl font-black mb-16 text-black uppercase tracking-tighter">Finance Learning Made Fun!</p>
            <NeoButton onClick={handleStartMusic} className="text-2xl px-12 py-4">{t.start}</NeoButton>
            <div className="mt-8"><button onClick={() => setIsMuted(!isMuted)} className="text-xs font-black uppercase underline">{isMuted ? 'Unmute' : 'Mute'}</button></div>
          </div>
        );

      case 'AUTH':
        return (
          <div className="min-h-screen flex flex-col items-center justify-center p-4 fade-in">
             <div className="w-full max-w-4xl grid grid-cols-1 md:grid-cols-2 gap-0 border-2 border-black neo-shadow-lg bg-white">
              <div className="p-8 md:p-12 flex flex-col justify-center border-b-2 md:border-b-0 md:border-r-2 border-black">
                <h2 className="text-4xl font-arcane mb-8 uppercase text-left">{isLoginMode ? t.login : t.signup}</h2>
                <div className="flex gap-2 mb-6 flex-wrap">
                  {['en', 'te', 'kn', 'ml', 'ta', 'hi'].map((l) => (
                    <button key={l} onClick={() => setProgress(p => ({...p, language: l as Language}))} className={`text-xs font-black uppercase px-2 py-1 border-2 border-black ${progress.language === l ? 'bg-[#90EE90]' : 'bg-white'}`}>{l.toUpperCase()}</button>
                  ))}
                </div>
                <form onSubmit={handleAuth} className="space-y-6">
                  <div>
                    <label className="font-black uppercase text-xs mb-1 block">{t.username}</label>
                    <input 
                      type="text" 
                      value={username} 
                      onChange={e => setUsername(e.target.value)} 
                      className="w-full border-2 border-black p-3 font-bold bg-white focus:bg-[#e6ffe6] outline-none transition-colors duration-300" 
                      required 
                    />
                  </div>
                  <div>
                    <label className="font-black uppercase text-xs mb-1 block">{t.password}</label>
                    <input 
                      type="password" 
                      value={password} 
                      onChange={e => setPassword(e.target.value)} 
                      className="w-full border-2 border-black p-3 font-bold bg-white focus:bg-[#e6ffe6] outline-none transition-colors duration-300" 
                      required 
                    />
                  </div>
                  <NeoButton type="submit" className="w-full py-4 text-xl mt-4">{isLoginMode ? t.enter : t.join} →</NeoButton>
                </form>
              </div>
              <div className={`p-8 md:p-12 flex flex-col items-center justify-center text-center ${!isLoginMode ? 'bg-[#90EE90]' : 'bg-yellow-100'}`}>
                 <h3 className="text-3xl font-black uppercase mb-4">{isLoginMode ? t.newHere : t.welcome}</h3>
                 <button onClick={() => setIsLoginMode(!isLoginMode)} className="bg-white border-2 border-black px-8 py-3 font-black uppercase neo-shadow">{isLoginMode ? t.createAccount : t.backToLogin}</button>
              </div>
            </div>
          </div>
        );

      case 'HOME':
         return (
          <div className="min-h-screen p-8 md:p-12 fade-in max-w-6xl mx-auto flex flex-col">
             <NavHeader />
             <div className="flex-grow flex flex-col items-center justify-center">
                <h2 className="text-6xl md:text-8xl font-arcane mb-16 text-center title-main uppercase">{t.commandCenter || "COMMAND CENTER"}</h2>
                <div className="grid grid-cols-1 md:grid-cols-2 gap-8 w-full max-w-4xl">
                   <button onClick={() => setView('ONBOARDING')} className="bg-white border-4 border-black p-12 neo-shadow hover:-translate-y-2 transition-all flex flex-col items-center gap-4 group">
                      <span className="text-6xl group-hover:scale-110 transition-transform">🗺️</span>
                      <span className="text-3xl font-black uppercase">{t.paths}</span>
                   </button>
                   <button onClick={() => setView('STORE')} className="bg-white border-4 border-black p-12 neo-shadow hover:-translate-y-2 transition-all flex flex-col items-center gap-4 group">
                      <span className="text-6xl group-hover:scale-110 transition-transform">🛒</span>
                      <span className="text-3xl font-black uppercase">{t.store}</span>
                   </button>
                   <button onClick={() => setView('PROFILE')} className="bg-white border-4 border-black p-12 neo-shadow hover:-translate-y-2 transition-all flex flex-col items-center gap-4 group">
                      <span className="text-6xl group-hover:scale-110 transition-transform">👤</span>
                      <span className="text-3xl font-black uppercase">{t.profile}</span>
                   </button>
                   <button onClick={() => setView('SETTINGS')} className="bg-white border-4 border-black p-12 neo-shadow hover:-translate-y-2 transition-all flex flex-col items-center gap-4 group">
                      <span className="text-6xl group-hover:scale-110 transition-transform">⚙️</span>
                      <span className="text-3xl font-black uppercase">{t.settings}</span>
                   </button>
                </div>
             </div>
          </div>
         );

      case 'SETTINGS':
         return (
            <div className="min-h-screen p-8 md:p-12 fade-in max-w-4xl mx-auto flex flex-col">
              <NavHeader />
              <div className="bg-white border-2 border-black p-8 md:p-12 neo-shadow-lg">
                 <h2 className="text-5xl font-arcane mb-8 uppercase border-b-2 border-black pb-4">{t.settings}</h2>
                 <div className="space-y-8">
                    <div>
                       <label className="block text-xl font-black uppercase mb-4">{t.language}</label>
                       <div className="grid grid-cols-2 md:grid-cols-3 gap-4">
                          {['en', 'te', 'kn', 'ml', 'ta', 'hi'].map((l) => (
                             <button 
                                key={l}
                                onClick={() => setProgress(p => ({...p, language: l as Language}))}
                                className={`p-4 border-2 border-black font-bold uppercase transition-all ${progress.language === l ? 'bg-[#90EE90] neo-shadow' : 'bg-white hover:bg-gray-50'}`}
                             >
                                {l === 'en' ? 'English' : 
                                 l === 'te' ? 'Telugu' :
                                 l === 'kn' ? 'Kannada' :
                                 l === 'ml' ? 'Malayalam' :
                                 l === 'ta' ? 'Tamil' : 'Hindi'}
                             </button>
                          ))}
                       </div>
                    </div>
                    
                    <div className="flex items-center justify-between border-t-2 border-black pt-8">
                       <label className="text-xl font-black uppercase">{t.notifications}</label>
                       <button className="w-16 h-8 border-2 border-black bg-[#90EE90] rounded-full relative neo-shadow">
                          <div className="absolute right-1 top-1 w-5 h-5 bg-black rounded-full"></div>
                       </button>
                    </div>

                    <div className="border-t-2 border-black pt-8">
                       <button onClick={handleLogout} className="text-red-500 font-black uppercase text-xl hover:underline">LOG OUT</button>
                    </div>
                 </div>
                 <NeoButton onClick={goToHub} className="w-full mt-12 py-4 text-xl">BACK TO HUB →</NeoButton>
              </div>
            </div>
         );

      case 'ONBOARDING':
        return (
          <div className="min-h-screen p-8 md:p-12 fade-in max-w-6xl mx-auto flex flex-col">
            <NavHeader />
            <h2 className="text-5xl font-arcane mb-4 uppercase text-center title-main">{t.choosePath}</h2>
            <p className="text-xl font-bold mb-12 text-center max-w-2xl mx-auto">{t.pathDesc}</p>
            
            {isReadyForRankUp && (
              <div className="mb-12 bg-white border-2 border-black p-8 neo-shadow-lg text-center animate-bounce-slight">
                <h3 className="text-3xl font-arcane mb-2 uppercase">{t.rankUpReady}</h3>
                <p className="mb-6 font-bold">{t.examDesc}</p>
                <NeoButton onClick={startRankExam} className="text-2xl py-4 w-full md:w-auto">{t.takeExam} →</NeoButton>
              </div>
            )}

            <div className={`grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8 w-full pb-12 ${isReadyForRankUp ? 'opacity-50 pointer-events-none' : ''}`}>
              {categories.map((cat) => {
                const isMastered = progress.completedCategoriesForCurrentRank.includes(cat.title);
                const catPerc = progress.categoryProgress[cat.title] || 0;

                return (
                  <button 
                    key={cat.id}
                    onClick={() => handleSelectCategory(cat)}
                    className="bg-white border-2 border-black p-6 text-left neo-shadow hover:-translate-y-2 transition-all group flex flex-col h-full relative overflow-hidden"
                  >
                    {isMastered && (
                      <div className="absolute inset-0 bg-[#90EE90]/90 z-10 flex items-center justify-center backdrop-blur-sm">
                        <div className="bg-black text-[#90EE90] px-4 py-2 border-2 border-black font-black text-xl -rotate-6 shadow-[4px_4px_0px_white]">
                          {t.mastered}
                        </div>
                      </div>
                    )}
                    <div className="flex justify-between items-start mb-4">
                      <div className="text-5xl group-hover:scale-110 transition-transform">{cat.icon}</div>
                      <div className="text-[10px] font-black uppercase px-2 py-1 border-2 border-black bg-yellow-100">
                        {currentRankName}
                      </div>
                    </div>
                    <h3 className="text-2xl font-black uppercase mb-2">{cat.title}</h3>
                    <p className="font-bold text-black/60 mb-6 flex-grow">{cat.description}</p>
                    <div className="mt-auto space-y-3">
                      <div className="flex justify-between items-center">
                         <span className="text-xs font-black uppercase">{t.progress}</span>
                         <span className="text-sm font-black">{isMastered ? 100 : catPerc}%</span>
                      </div>
                      <div className="h-4 w-full progress-bar-container">
                        <div className="progress-bar-inner" style={{ width: `${isMastered ? 100 : catPerc}%` }}></div>
                      </div>
                    </div>
                  </button>
                );
              })}
            </div>
          </div>
        );

      case 'STORE':
        return (
          <div className="min-h-screen p-8 md:p-12 fade-in max-w-6xl mx-auto flex flex-col">
            <NavHeader />
            <div className="flex justify-between items-end mb-8 border-b-2 border-black pb-4">
              <h2 className="text-5xl font-arcane uppercase title-main">{t.store}</h2>
              <div className="text-right">
                <span className="text-xs font-black uppercase block opacity-60">Your Balance</span>
                <span className="text-4xl font-black text-[#90EE90] drop-shadow-[1px_1px_0_black]">{progress.xp} XP</span>
              </div>
            </div>

            <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
              {STORE_ITEMS.map((item) => {
                const isOwned = (progress.inventory || []).includes(item.id);
                const isEquipped = progress.equippedBanner === item.id;
                const canAfford = progress.xp >= item.cost;

                return (
                  <div key={item.id} className="bg-white border-2 border-black p-6 flex flex-col neo-shadow hover:-translate-y-1 transition-all">
                    <div className={`h-24 w-full border-2 border-black mb-4 flex items-center justify-center overflow-hidden relative ${item.type === 'banner' ? item.value : 'bg-gray-50'}`}>
                       {item.type === 'badge' && <span className="text-5xl">{item.value}</span>}
                       {item.type === 'banner' && <span className="text-xs font-black uppercase bg-white px-2 border-2 border-black z-10">Preview</span>}
                    </div>
                    
                    <div className="flex justify-between items-start mb-2">
                       <h3 className="text-xl font-black uppercase">{item.name}</h3>
                       <span className="text-xs font-bold bg-black text-white px-2 py-1 uppercase">{item.type}</span>
                    </div>
                    <p className="text-sm font-bold opacity-60 mb-6 flex-grow">{item.description}</p>
                    
                    <div className="mt-auto">
                      {isOwned ? (
                         item.type === 'banner' ? (
                           <button 
                             onClick={() => handleEquipItem(item)}
                             disabled={isEquipped}
                             className={`w-full py-3 border-2 border-black font-black uppercase transition-all ${isEquipped ? 'bg-[#90EE90] opacity-50 cursor-default' : 'bg-white hover:bg-gray-100'}`}
                           >
                             {isEquipped ? t.equipped : t.equip}
                           </button>
                         ) : (
                           <div className="w-full py-3 border-2 border-black bg-gray-100 font-black uppercase text-center opacity-50 cursor-default">{t.owned}</div>
                         )
                      ) : (
                        <button 
                           onClick={() => handleBuyItem(item)}
                           disabled={!canAfford}
                           className={`w-full py-3 border-2 border-black font-black uppercase transition-all flex justify-between px-4 ${canAfford ? 'bg-yellow-300 hover:bg-yellow-400 neo-button' : 'bg-gray-200 opacity-50 cursor-not-allowed'}`}
                        >
                          <span>{t.buy}</span>
                          <span>{item.cost} XP</span>
                        </button>
                      )}
                    </div>
                  </div>
                );
              })}
            </div>
          </div>
        );
      
      case 'RANK_EXAM':
        const question = rankExamQuestions[currentExamQuestionIndex];
        return (
          <div className="min-h-screen p-8 md:p-12 fade-in max-w-3xl mx-auto flex flex-col justify-center">
            <div className="bg-white border-4 border-black p-8 md:p-12 neo-shadow-lg relative">
               <div className="absolute -top-6 left-1/2 -translate-x-1/2 bg-black text-[#90EE90] px-6 py-2 border-2 border-white font-black text-xl uppercase tracking-widest">
                  RANK UP EXAM
               </div>
               <div className="flex justify-between mb-8 text-xs font-black uppercase text-black/40">
                 <span>Question {currentExamQuestionIndex + 1} of {rankExamQuestions.length}</span>
                 <span>Target: {RANK_ORDER[progress.rankIndex + 1]}</span>
               </div>
               <h2 className="text-2xl md:text-3xl font-bold mb-8">{question.question}</h2>
               <div className="grid gap-4">
                 {question.options.map((opt, i) => (
                   <button 
                     key={i}
                     onClick={() => submitExamAnswer(i)}
                     className="p-4 border-2 border-black text-left font-bold hover:bg-[#90EE90] transition-colors neo-shadow active:translate-y-1 active:shadow-none"
                   >
                     {opt}
                   </button>
                 ))}
               </div>
            </div>
          </div>
        );

      case 'RANK_SUCCESS':
        return (
           <div className="min-h-screen flex flex-col items-center justify-center p-4 fade-in bg-yellow-300">
              <div className="bg-white border-8 border-black p-12 neo-shadow-lg text-center max-w-2xl rotate-2 animate-bounce-slight">
                 <div className="text-8xl mb-6">🏆</div>
                 <h2 className="text-5xl md:text-7xl font-arcane mb-4 uppercase">RANK UP!</h2>
                 <p className="text-2xl font-bold mb-8">You are now a {currentRankName}</p>
                 <div className="text-lg font-bold mb-8 opacity-60">
                    Your path progress has been reset.<br/>New challenges await at this higher difficulty level.
                 </div>
                 <NeoButton onClick={goToHub} className="text-2xl py-4 w-full">CONTINUE JOURNEY →</NeoButton>
              </div>
           </div>
        );

      case 'LOADING':
         return (
          <div className="min-h-screen flex flex-col items-center justify-center p-4 fade-in bg-[#AFEEEE]">
            <div className="flex flex-col items-center gap-12 text-center max-w-md">
              <div className="gemini-container"><div className="gemini-star-wrapper"><div className="gemini-star-inner"></div></div></div>
              <div className="space-y-4">
                <h2 className="text-4xl font-arcane uppercase tracking-widest title-main">GENERATING CONTENT</h2>
                <p className="font-black uppercase tracking-[0.2em] text-black/60 text-sm">Consulting the Financial Spirits...</p>
              </div>
            </div>
          </div>
        );

      case 'ROADMAP':
         return (
          <div className={`min-h-screen p-8 md:p-12 fade-in max-w-4xl mx-auto transition-colors duration-300 ${isCurrentRoadmapComplete ? 'completed-theme' : ''}`}>
            {isCurrentRoadmapComplete && (
              <div className="fixed inset-0 z-50 flex items-center justify-center roadmap-completed-overlay">
                 <div className="bg-[#90EE90] border-8 border-black p-12 neo-shadow-lg text-center rotate-[-2deg] fade-in max-w-lg">
                    <h2 className="text-6xl font-arcane mb-4 uppercase text-black">{t.mastered}</h2>
                    <p className="text-xl font-bold mb-2 text-black">{t.pathBonus}</p>
                    <NeoButton onClick={goToPaths} className="text-2xl py-4 mt-8">{t.choosePath} →</NeoButton>
                 </div>
              </div>
            )}
            <NavHeader />
            <div className="mb-12 border-2 border-black p-8 neo-shadow bg-white/60 backdrop-blur-md">
              <div className="flex justify-between items-center mb-4">
                <h3 className="text-2xl font-black uppercase tracking-tight">{currentCategory} ({currentRankName})</h3>
                <span className="text-4xl font-arcane text-[#90EE90] drop-shadow-[2px_2px_0_rgba(0,0,0,1)]">{roadmapProgress}%</span>
              </div>
              <div className="h-10 w-full progress-bar-container">
                <div className="progress-bar-inner" style={{ width: `${roadmapProgress}%` }}></div>
              </div>
            </div>
            <div className="space-y-16">
              {roadmap.map((lvl) => (
                <div key={lvl.id} className="relative">
                  <div className="flex items-center gap-4 mb-8">
                    <div className="bg-black text-[#90EE90] w-16 h-16 flex items-center justify-center border-2 border-black neo-shadow font-black text-2xl rotate-[-3deg]">{lvl.id}</div>
                    <h3 className="text-4xl font-black uppercase">{lvl.title}</h3>
                  </div>
                  <div className="grid grid-cols-1 md:grid-cols-2 gap-8 relative">
                    {lvl.lessons.map((lesson) => {
                      const allLessons = roadmap.flatMap(l => l.lessons);
                      const currentIdx = allLessons.indexOf(lesson);
                      const isAccessible = currentIdx === 0 || progress.completedLessonIds.includes(allLessons[currentIdx - 1].id);
                      const isCompleted = progress.completedLessonIds.includes(lesson.id);
                      return (
                        <button key={lesson.id} disabled={!isAccessible} onClick={() => { setActiveLesson(lesson); setView('LESSON'); }} className={`p-6 border-2 border-black text-left relative transition-all ${isAccessible ? 'bg-white neo-shadow hover:-translate-y-2' : 'bg-gray-200 opacity-50 cursor-not-allowed'} ${isCompleted ? 'bg-[#90EE90]/20' : ''}`}>
                          <div className="flex justify-between items-start mb-3">
                            <span className="text-xs font-black uppercase tracking-widest text-black/40">Task {currentIdx + 1}</span>
                            {isCompleted && <span className="text-xs bg-black text-[#90EE90] px-2 py-1 font-bold uppercase">{t.mastered}</span>}
                          </div>
                          <h4 className="text-2xl font-bold uppercase leading-tight">{lesson.title}</h4>
                          {!isAccessible && <div className="absolute inset-0 flex items-center justify-center bg-black/5 backdrop-blur-[1px]"><span className="text-5xl">🔒</span></div>}
                        </button>
                      );
                    })}
                  </div>
                </div>
              ))}
            </div>
          </div>
         );

      case 'LESSON':
        return activeLesson && (
          <div className="min-h-screen p-8 md:p-12 fade-in max-w-3xl mx-auto flex flex-col justify-center">
            <NavHeader />
            <div className="bg-white border-2 border-black p-8 md:p-12 neo-shadow-lg">
              <h2 className="text-xs font-black uppercase tracking-[0.2em] mb-4 text-black/40 border-b-2 border-black/10 pb-2">Topic Explorer • {activeLesson.title}</h2>
              <div className="text-3xl md:text-4xl font-bold leading-tight mb-8">{activeLesson.content}</div>
              {activeLesson.resources && activeLesson.resources.length > 0 && (
                <div className="mb-8 p-6 bg-blue-50 border-2 border-black border-dashed">
                   <h3 className="text-sm font-black uppercase mb-4 flex items-center gap-2"><span>📚</span> {t.resources}</h3>
                   <div className="space-y-3">{activeLesson.resources.map((res, idx) => (
                        <a key={idx} href={res.url} target="_blank" rel="noreferrer" className="flex items-center gap-3 p-3 bg-white border-2 border-black hover:bg-[#90EE90] transition-colors">
                           <span className="text-xl">{res.type === 'Video' ? '📺' : res.type === 'Tool' ? '🛠️' : '📰'}</span>
                           <span className="font-bold underline">{res.title}</span>
                        </a>
                   ))}</div>
                </div>
              )}
              <div className="flex gap-6">
                <NeoButton onClick={() => setView('QUIZ')} className="flex-1 py-6 text-3xl">{t.begin} →</NeoButton>
                <NeoButton variant="secondary" onClick={() => setView('ROADMAP')}>{t.retreat}</NeoButton>
              </div>
            </div>
          </div>
        );

      case 'QUIZ':
        return activeLesson && (
          <div className="min-h-screen p-8 md:p-12 fade-in max-w-2xl mx-auto flex flex-col justify-center">
            <NavHeader />
            <div className="bg-white border-2 border-black p-8 neo-shadow-lg">
              <h2 className="text-3xl font-black uppercase mb-8 border-b-2 border-black pb-4">Trial of Knowledge</h2>
              <p className="text-3xl font-bold mb-10">{activeLesson.quiz.question}</p>
              <div className="grid gap-6 mb-10">
                {activeLesson.quiz.options.map((opt, i) => (
                  <button key={i} onClick={() => setSelectedOption(i)} className={`p-6 border-2 border-black font-bold text-left text-xl transition-all ${selectedOption === i ? 'bg-[#90EE90] translate-x-2 translate-y-2 shadow-none' : 'bg-white neo-shadow active:translate-x-1 active:translate-y-1'}`}>
                    <span className="mr-4 opacity-40">{String.fromCharCode(65 + i)}.</span> {opt}
                  </button>
                ))}
              </div>
              <NeoButton onClick={handleFinishQuiz} className="w-full py-6 text-2xl" disabled={selectedOption === null}>{t.submit} →</NeoButton>
            </div>
          </div>
        );

      case 'QUIZ_SUMMARY':
        return (
          <div className="min-h-screen p-8 md:p-12 fade-in max-w-2xl mx-auto flex flex-col justify-center">
            <div className={`bg-white border-8 border-black p-12 neo-shadow-lg text-center ${quizSuccess ? 'rotate-[1deg]' : 'rotate-[-1deg]'}`}>
              <div className="mb-8">{quizSuccess ? <div className="text-9xl mb-4">✨</div> : <div className="text-9xl mb-4">💀</div>}</div>
              <h2 className="text-6xl font-arcane mb-4 uppercase">{quizSuccess ? t.passed : t.failed}</h2>
              <p className="text-2xl font-bold mb-8 uppercase">{quizSuccess ? `${t.xpGranted} (+${earnedXp} XP)` : t.tryAgain}</p>
              <div className="space-y-6">
                <NeoButton onClick={() => { setSelectedOption(null); setView('ROADMAP'); }} className="w-full py-5 text-2xl">{quizSuccess ? t.continue : t.backRoadmap}</NeoButton>
                {!quizSuccess && <button onClick={() => setView('LESSON')} className="text-lg font-black uppercase hover:underline">{t.reread}</button>}
              </div>
            </div>
          </div>
        );
        
      case 'PROFILE':
        const currentBanner = STORE_ITEMS.find(i => i.id === progress.equippedBanner);
        return (
          <div className="min-h-screen p-8 md:p-12 fade-in max-w-4xl mx-auto flex flex-col">
            <NavHeader />
            <div className="bg-white border-2 border-black p-8 neo-shadow-lg">
              {/* Profile Banner */}
              <div className={`w-full h-32 border-2 border-black mb-8 relative overflow-hidden ${currentBanner ? currentBanner.value : 'bg-[#90EE90]/20'}`}>
                 <div className="absolute bottom-2 left-4 bg-white border-2 border-black px-2 py-1 text-xs font-black uppercase z-10">
                   {currentBanner ? currentBanner.name : 'Standard Banner'}
                 </div>
              </div>

              <h2 className="text-4xl font-arcane mb-8 uppercase border-b-2 border-black pb-4">{t.profile}</h2>
              <div className="grid grid-cols-1 md:grid-cols-2 gap-12">
                <div className="space-y-6">
                  <div className="border-2 border-black p-6 neo-shadow bg-[#90EE90]/20">
                    <p className="text-xs font-black uppercase text-black/40 mb-1">Current Rank</p>
                    <p className="text-4xl font-arcane mb-2">{currentRankName}</p>
                    <div className="h-6 w-full progress-bar-container mb-2">
                      <div className="progress-bar-inner" style={{ width: `${rankCompletionPercentage}%` }}></div>
                    </div>
                    <p className="text-xs font-bold text-right">{progress.completedCategoriesForCurrentRank.length}/{categories.length} Paths Completed</p>
                    <p className="mt-4 font-bold text-2xl">{progress.xp} XP</p>
                  </div>
                  <div className="border-2 border-black p-4 neo-shadow">
                    <p className="text-xs font-black uppercase text-black/40 mb-3">Badges & Inventory</p>
                    <div className="flex flex-wrap gap-3">
                      {progress.badges.length > 0 ? progress.badges.map((b, i) => (
                        <div key={i} className="bg-yellow-400 border-2 border-black p-2 neo-shadow font-bold text-xs uppercase flex items-center gap-2">🏅 {b}</div>
                      )) : null}
                      {/* Show Bought Badges */}
                      {(progress.inventory || []).filter(id => id.startsWith('badge')).map((badgeId, i) => {
                         const item = STORE_ITEMS.find(it => it.id === badgeId);
                         return item ? (
                           <div key={`inv_${i}`} className="bg-white border-2 border-black p-2 neo-shadow font-bold text-xs uppercase flex items-center gap-2" title={item.name}>
                             {item.value}
                           </div>
                         ) : null;
                      })}
                      {progress.badges.length === 0 && (!progress.inventory || !progress.inventory.some(i => i.startsWith('badge'))) && <p className="font-bold text-black/30">No badges yet.</p>}
                    </div>
                  </div>
                </div>
              </div>
              <NeoButton onClick={goToHub} className="w-full mt-12 py-4 text-xl">BACK TO HUB →</NeoButton>
            </div>
          </div>
        );

      default:
        return null;
    }
  };

  return (
    <div className={`min-h-screen text-black transition-colors duration-300 ${isCurrentRoadmapComplete && view === 'ROADMAP' ? 'completed-theme' : ''}`}>
      <GeometricBackground />
      <div className="relative z-10">
        {renderContent()}
      </div>
    </div>
  );
};

export default App;